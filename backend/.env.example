# Solana RPC endpoints
RPC_URL="https://api.devnet.solana.com"
WS_URL="wss://api.devnet.solana.com"

# Path to the oracle authority Ed25519 keypair (must match on-chain VrfConfiguration.authority)
AUTHORITY_KEYPAIR_PATH="~/.config/solana/id.json"

# HMAC-SHA256 secret key for deterministic randomness derivation (hex-encoded)
# Generate with: openssl rand -hex 32
HMAC_SECRET="your-hmac-secret-hex-here"

# Deployed VRF program ID
PROGRAM_ID="A4pDDsKvtX2U3jyEURVSoH15Mx4JcgUiSqCKxqWE3N48"

# Cluster name for explorer URLs: "devnet" or "mainnet-beta"
# CLUSTER="devnet"

# HTTP server port for health/status/metrics endpoints (default: 8080)
# HTTP_PORT=8080

# Maximum fulfillment retry attempts on BlockhashNotFound (default: 5)
# MAX_RETRIES=5

# Initial retry delay in milliseconds, doubles each attempt (default: 500)
# INITIAL_RETRY_DELAY_MS=500

# Priority fee in micro-lamports per compute unit; 0 = no priority fee (default: 0)
# PRIORITY_FEE_MICRO_LAMPORTS=0

# Maximum concurrent fulfillment tasks (default: 4)
# FULFILLMENT_CONCURRENCY=4

# Photon indexer RPC URL for ZK Compressed request support (optional).
# When set, the backend will also listen for and fulfill compressed requests.
# Helius devnet: https://devnet.helius-rpc.com/?api-key=<your-key>
# PHOTON_RPC_URL="https://devnet.helius-rpc.com/?api-key=your-api-key"
